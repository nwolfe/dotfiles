#!/bin/bash

command="puppet agent --no-daemonize --debug --trace --verbose --confdir=$PUPPETLABS_CONF/agent/conf --vardir=$PUPPETLABS_CONF/agent/var --server localhost --onetime --certname my-agent"
echo $command
echo -n $command | pbcopy
